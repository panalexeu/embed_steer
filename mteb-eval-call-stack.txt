# Just a note on the eval call stack structure for myself

MTEB.run
 └── _run_eval (MTEB)
      └── evaluate (AbsTaskRetrieval)
           ├── new RetrievingEvaluator
           │     └── new DenseRetrievalExactSearch
           └── _evaluate_subset (AbsTaskRetrieval)
                └── __call__ (RetrievingEvaluator)
                     └── search (DenseRetrievalExactSearch)
